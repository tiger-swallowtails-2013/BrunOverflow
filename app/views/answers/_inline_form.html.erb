<%= form_for(@answer, remote: true) do |form| %>
  <%= form.label :body %><%= form.text_field :body %>
  <%= form.hidden_field :question_id, value: "#{@question.id}" %>
  <%= form.submit value: "create new comment" %>
<% end %>
